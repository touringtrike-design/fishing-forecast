# PostgreSQL Production Environment
# Copy this to .env and modify for production use

# PostgreSQL connection (PostGIS enabled)
DATABASE_URL=postgresql://fishing_user:fishing_password@localhost:5432/fishing_forecast

# Backend server
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8080

# Frontend server
FRONTEND_PORT=3001

# JWT Secret (IMPORTANT: Change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# API Keys (optional)
# OPEN_METEO_API_KEY=your_key_here
